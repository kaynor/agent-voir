import { ComingSoon } from "../components/ComingSoon";

export const metadata = { title: "Tools | AgentVoir" };

export default function ToolsPage() {
  return (
    <ComingSoon
      title="Tools & MCP servers"
      description="Browse registered tools, MCP servers, and tool-call audit history."
    />
  );
}
