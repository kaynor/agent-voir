import { ComingSoon } from "../components/ComingSoon";

export const metadata = { title: "Settings | AgentVoir" };

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="OIDC login, export integrations, saved views, and tenant preferences."
    />
  );
}
