import { ComingSoon } from "../components/ComingSoon";

export const metadata = { title: "Audit Logs | AgentVoir" };

export default function AuditPage() {
  return (
    <ComingSoon
      title="Audit logs"
      description="Tamper-evident history of registry changes, policy denials, and budget blocks."
    />
  );
}
