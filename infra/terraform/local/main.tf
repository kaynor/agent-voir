terraform {
  required_version = ">= 1.7.0"
}

# Placeholder for local/cloud infrastructure examples.
# Future modules may provision Postgres, Redis, ClickHouse, OIDC clients,
# Kubernetes namespaces, and secrets backends.
