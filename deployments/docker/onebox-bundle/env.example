# Optional overrides — copy to .env in this folder if needed.
# Defaults work for a first try (see .version for the pinned release tag).

# AGENTVOIR_IMAGE=ghcr.io/kaynor/agent-voir
# AGENTVOIR_VERSION=v0.2.4

# AGENTVOIR_GATEWAY_PORT=8080
# AGENTVOIR_REGISTRY_PORT=8081
# AGENTVOIR_USAGE_PORT=8082

# GATEWAY_API_KEY=agentvoir-onebox-key
# OPENAI_API_KEY=
